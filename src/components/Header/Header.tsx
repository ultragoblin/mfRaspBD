import React from "react";
import {CustomSelectInterface} from "@components/CustomSelect";
import "./Header.scss";

const selectWidth: number = 167;

const selects: CustomSelectInterface[] = [
  {
    width: selectWidth,
    id: {

    },
    label: {
      '2'
    },
  }
];

const Header = () => {

  return (
    <header>
      
    </header>
  );
};

export default Header;
